# ENV VARIABLES - DOCKER SETUP ONLY
# EACH RESPECTIVE PACKAGE NEEDS THEIR OWN ENV VARIABLES FOUND IN THEIR RESPECTIVE SCOPES

# PORTS
CLIENT_PORT = 3000
SERVER_PORT = 4000

# CLIENT
SERVER = http://localhost:4000
SECRET_API_KEY = secretapikey

# SERVER
CLIENT = localhost
PORT = 4000
NODE_ENV = dev
RATE_TTL = 50000000
RATE_LIMIT = 50000
SECRET_API_KEY = secretapikey

# NODE MAILER
APP_PASSWORD = yourapppassword
USER_EMAIL = email
MAIL_HOST= host
MAIL_HOST_PORT = port

# POSTGRES DB
PGHOST = postgres_db
PGDATABASE = ppw_api_test
PGUSERNAME = postgres
PGPASSWORD = 1234
PGPORT = 5432
ADMINER_PORT = 8080
DATABASE_URL = postgres://${PGUSERNAME}:${PGPASSWORD}@${PGHOST}:${PGPORT}/${PGDATABASE}
